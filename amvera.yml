meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands: npm install && npm install https-proxy-agent node-fetch@2 && npm run build
  artifacts:
    "*": /
run:
  scriptName: null
  scriptArguments: null
  nodeArguments: null
  command: node server.js
  persistenceMount: /data
  containerPort: "80"
