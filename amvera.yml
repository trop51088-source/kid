meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands: npm run build
  artifacts:
    "*": /
run:
  command: npx serve -s dist -l 80
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
