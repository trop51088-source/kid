meta:
  environment: nodejs
  toolchain:
    name: node
    version: "20"

build:
  dockerfile: false
  buildCommand: npm install && npm run build

run:
  command: npm run preview -- --host 0.0.0.0 --port 3000
